image: ubuntu:22.04

stages:
  - tests

tests:
  stage: tests
  before_script:
    - apt update && apt install -y python3-pip
    - pip install pytest
    - pip install .
  script:
    - pytest tests/
  tags:
    - public-runner-docker
