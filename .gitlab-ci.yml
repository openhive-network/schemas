image: ubuntu:22.04

stages:
  - tests

tests:
  stage: tests
  before_script:
    - apt update && apt install -y python3-pip
    - pip install .[test]
  script:
    - pytest tests/
  tags:
    - public-runner-docker
