image: docker:stable

stages:
  - tests

tests:
  stage: tests
  before_script:
    - apk update
    - apk add py3-pip
    - pip3 install pytest
    - pip3 install .
  script:
    - pytest tests/
  tags:
    - public-runner-docker
